twist_mux:
  ros__parameters:
    topics:
      joystick:
        topic: /cmd_vel_joy
        timeout: 0.5
        priority: 100         # 사람이 개입하면 무조건 우선
      navigation:
        topic: /cmd_vel_nav
        timeout: 0.5
        priority: 10